# Access system

Access system governs authentication and authorization — globally and per-chat.
See the full list of capabilities in `caps.py`.

(For this fixture run, `caps.py` carries the breadcrumb: `# System doc: docs/systems/access.md`. The fixture asserts that a file link is acceptable when the target carries a backlink.)
