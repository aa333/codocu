# Tech debt

Deferred items, known issues, and explicitly suboptimal choices. Active
in-flight work lives in `docs/plans/`. Add freely; remove on fix.

<!-- Format:
**<TD-short-id>**: <what is wrong> - <optional: reason for not fixing || solution draft>

Group by release once one is named (e.g. ## 0.2.0).
-->
